#!/usr/bin/env ruby

require "pegasus"

puts "Pegasus framework"

instruction = ARGV[0]
param1 = ARGV[1]

if (instruction == '--version' || instruction == '-v')
  Pegasus.version
elsif (instruction == '--init')
  Pegasus::Init.init param1
end
